/* Landing motion: scene focus, card tilt, reveals, spirit motes. */
(()=>{const b=document.body,rm=matchMedia('(prefers-reduced-motion: reduce)').matches;document.querySelectorAll('.build').forEach(c=>{const k=c.classList.contains('sf')?'sf':c.classList.contains('or')?'or':'';if(k){c.addEventListener('pointerenter',()=>b.dataset.focus=k);c.addEventListener('pointerleave',()=>delete b.dataset.focus);c.addEventListener('focusin',()=>b.dataset.focus=k);c.addEventListener('focusout',()=>delete b.dataset.focus);}if(!rm&&matchMedia('(pointer:fine)').matches){const a=c.querySelector('.build-art');c.addEventListener('pointermove',e=>{const r=c.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;a.style.transform=`translate3d(${x*14}px,${y*10}px,0) rotateY(${x*10}deg) rotateX(${-y*8}deg)`;});c.addEventListener('pointerleave',()=>a.style.transform='');}});if('IntersectionObserver'in window&&!rm){document.documentElement.classList.add('rv-on');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('shown');io.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.build,.steps article,.knowledge,.section-heading,.about').forEach((el,i)=>{el.style.transitionDelay=(i%4)*70+'ms';io.observe(el)});}
if('IntersectionObserver'in window){const po=new IntersectionObserver(es=>es.forEach(e=>e.target.classList.toggle('off',!e.isIntersecting)),{rootMargin:'80px'});document.querySelectorAll('.build').forEach(c=>po.observe(c));}const cv=document.getElementById('motes');if(!cv||rm||document.documentElement.classList.contains('lite'))return;const x=cv.getContext('2d');let W,H,P=[];const cols=[[95,227,193],[208,68,58],[165,141,255],[115,217,255],[236,214,163]];const sz=()=>{const d=Math.min(2,devicePixelRatio||1);W=innerWidth;H=innerHeight;cv.width=W*d;cv.height=H*d;x.setTransform(d,0,0,d,0,0);const n=Math.round(Math.min(60,W*H/30000));while(P.length<n)P.push(sp(1));P.length=n;};const sp=f=>{const left=Math.random()<.5;return{x:(left?0:W/2)+Math.random()*W/2,y:f?Math.random()*H:H+10,r:.8+Math.random()*2,vy:-(.1+Math.random()*.35),ph:Math.random()*6,c:left?cols[Math.random()<.75?0:(Math.random()<.5?1:4)]:cols[Math.random()<.6?2:(Math.random()<.6?3:1)],a:.2+Math.random()*.5}};sz();addEventListener('resize',sz,{passive:true});let last=0;(function t(now){requestAnimationFrame(t);if(now-last<30)return;const k=Math.min(4,(now-last)/16.67||1);last=now;x.clearRect(0,0,W,H);x.globalCompositeOperation='lighter';for(const p of P){p.ph+=.01*k;p.y+=p.vy*k;p.x+=Math.sin(p.ph)*.3*k;if(p.y<-20)Object.assign(p,sp(0));const g=x.createRadialGradient(p.x,p.y,0,p.x,p.y,p.r*7);g.addColorStop(0,`rgba(${p.c},${p.a})`);g.addColorStop(1,`rgba(${p.c},0)`);x.fillStyle=g;x.beginPath();x.arc(p.x,p.y,p.r*7,0,6.3);x.fill();}})();})();
